version: "3.4"
services:
  
  app:
    build: .

  db:
    image: postgres:9.6
    environment:
      - POSTGRES_DB=webui
      - POSTGRES_USER=webui
      - POSTGRES_PASSWORD=secure_password
    volumes:
      - ./pgdata-webui:/var/lib/postgresql/db


