version: "3.4"
services:
  
#  front:
#    build:
#      context: front
#
#  back:
#    build:
#      context: back
#    env_file:
#      - back/env
#    ports:
#      - 3000:3000
#    restart: always

  db:
    image: postgres:9.6
    environment:
      - POSTGRES_DB=webui
      - POSTGRES_USER=webui
      - POSTGRES_PASSWORD=secure_password
    volumes:
      - ./pgdata-webui:/var/lib/postgresql/db
    restart: always
    ports:
      - 5432:5432
